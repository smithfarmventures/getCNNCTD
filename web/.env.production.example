# Production env vars for the Vercel deployment.
# Set these in the Vercel dashboard: Project → Settings → Environment Variables.
# Do NOT commit the filled-in version.

# The public URL of the API deployed on Railway.
# Example: https://cnnctd-api-production.up.railway.app
RAILWAY_API_URL=
